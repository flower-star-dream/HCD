spring:
  application:
    name: hcd-trainSeat
  profiles:
    active: dev
  config:
    import:
      - nacos:hcd-common
  cloud:
    nacos:
      server-addr: localhost:8847
      username: nacos
      password: nacos
      config:
        namespace: 8a19b7dd-7c6e-4a98-8bcf-b81ebd4aad2c
        group: DEFAULT_GROUP
        file-extension: yaml
      discovery:
        ip: 127.0.0.1
        port: ${server.port}
        namespace: 8a19b7dd-7c6e-4a98-8bcf-b81ebd4aad2c
        group: DEFAULT_GROUP
  datasource:
    url: jdbc:mysql://localhost:3306/hcd_train_seat?useUnicode=true&characterEncoding=utf8&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

# 设置端口
server:
  port: 8082
